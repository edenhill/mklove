#!/bin/bash
#
# Build OS: Linux support
#

mkl_require cc

for n in CPPFLAGS LDFLAGS ; do
    mkl_mkvar_append $n $n "-fPIC"
done

mkl_mkvar_append LIB_LDFLAGS LIB_LDFLAGS '-shared'
mkl_mkvar_append LIB_LDFLAGS LIB_LDFLAGS '-Wl,-soname,$(LIBNAME).so.$(LIBVER)'
